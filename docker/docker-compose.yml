version: "3.9"

services:
  app:
    container_name: sig-service
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
    image: sig-service:ubuntu20
    network_mode: host
    restart: unless-stopped